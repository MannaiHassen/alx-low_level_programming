#include "main.h"

/**
 * _abs - This function return the absolute of an integer
 * @n: integer to check
 * Return: The absolute value of an integer
*/
int _abs(int n)
{
if (n < 0)
{
return (-n);
}
return (n);
}
